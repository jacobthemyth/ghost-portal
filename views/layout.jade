doctype html
html(lang=en)
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width = device-width")
    title Ghost Portal
    style(type="text/css").
      body {
        background: black;
        color: white;
        font-family: "Courier New", Courier, monospace;
      }
      ul {
        font-weight: bold;
      }

      a {
        color: white;
      }

      a:visited {
        color: white;
      }
  body
    block content
